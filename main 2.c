#include <kipr/wombat.h>

int main()
{
    create_connect();
    while (1)//get_create_rclightbump()==0)
    {
        printf("sensor: %d\n", get_create_lfcliff()); 
        msleep(500);   


    }
    printf ("Andrew can acually programmm buttt not spel guwd!");
    create_disconnect();
    return 0;
}
